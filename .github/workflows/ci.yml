name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    uses: nocoo/ci/.github/workflows/bun-quality.yml@main
    with:
      enable-g1: true
      enable-g2: true
      enable-l1: true
