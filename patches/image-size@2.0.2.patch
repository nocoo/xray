diff --git a/dist/fromFile.cjs b/dist/fromFile.cjs
index c226d3dac07f235db456c774b26cca88f655e1af..65c5b740c5d1371cef0a00979862e1befbc0d1e0 100644
--- a/dist/fromFile.cjs
+++ b/dist/fromFile.cjs
@@ -187,6 +187,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -277,9 +280,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -511,6 +522,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/fromFile.mjs b/dist/fromFile.mjs
index 4a3c44432982397204f7a9d04fc66ef65a8e7ba4..adec69031afa2040ce3632430a0fbd3be3f5f9bb 100644
--- a/dist/fromFile.mjs
+++ b/dist/fromFile.mjs
@@ -164,6 +164,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -254,9 +257,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -488,6 +499,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210eb131db06ccca30a5c466c7252a03af50..0c663b21f7fcc7979d5bf0763f4d508d90fa8c08 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -163,6 +163,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -253,9 +256,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -487,6 +498,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17afad4e406eba013fff7858366cb5f8e3ba4..5e4a0647af59407d8afc728f058bba05765eada6 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -159,6 +159,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,9 +252,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -483,6 +494,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/lookup.cjs b/dist/lookup.cjs
index 9731ad8bb0949f8fdf9297851bdb312e91282f01..54e2df22c83cfe6ab6bac19f2b0b941b56c7c7b9 100644
--- a/dist/lookup.cjs
+++ b/dist/lookup.cjs
@@ -161,6 +161,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -251,9 +254,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -485,6 +496,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/lookup.mjs b/dist/lookup.mjs
index f787d1c8408d7b681fec8617595d058f66959869..3f7fc7689fe4ba9444c9d27f21f627bc0f3c1036 100644
--- a/dist/lookup.mjs
+++ b/dist/lookup.mjs
@@ -159,6 +159,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,9 +252,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -483,6 +494,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f490788ea654ea9896060a5c0253d81e348af92..a1887118011810e60f2f961012735f477df37d9c 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -59,6 +59,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 01330919e01b3c034a96a7b4b1dbf5a7f658bb2d..7d97fc887bb095c80efa0e3a78360740ae0ae497 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -57,6 +57,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d370dcf80a10f14a6deebfdbe04d3c70585c..fe0d88808e16412e4c90bd9833222806d5e512f2 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -72,9 +72,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027bc5890a79d05a7fdb87de3018e556729e..1cce38b58b04cf533ec604a8bd766ef85e2b74f9 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -70,9 +70,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
diff --git a/dist/types/index.cjs b/dist/types/index.cjs
index 6949cd134db7ab59dae7b804b36c7bacb4e387cf..82cd52bc202e757c9026dfa42ee45af0698f3de4 100644
--- a/dist/types/index.cjs
+++ b/dist/types/index.cjs
@@ -161,6 +161,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -251,9 +254,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -485,6 +496,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/index.mjs b/dist/types/index.mjs
index b026dda4aa16c6077a76e4b3cfe3f54fe1d6d1d0..e191aeee4c1d9083be0e58193040c86adb7a2216 100644
--- a/dist/types/index.mjs
+++ b/dist/types/index.mjs
@@ -159,6 +159,9 @@ var HEIF = {
     while (currentOffset < ipcoBox.offset + ipcoBox.size) {
       const ispeBox = findBox(input, "ispe", currentOffset);
       if (!ispeBox) break;
+      if (ispeBox.size === 0) {
+        throw new TypeError("Invalid HEIF, zero-sized ispe box");
+      }
       const rawWidth = readUInt32BE(input, ispeBox.offset + 12);
       const rawHeight = readUInt32BE(input, ispeBox.offset + 16);
       const clapBox = findBox(input, "clap", currentOffset);
@@ -249,9 +252,17 @@ var ICNS = {
     const images = [];
     while (imageOffset < fileLength && imageOffset < inputLength) {
       const imageHeader = readImageHeader(input, imageOffset);
+      const imageLength = imageHeader[1];
+      if (
+        imageLength < SIZE_HEADER2 ||
+        imageOffset + imageLength > fileLength ||
+        imageOffset + imageLength > inputLength
+      ) {
+        throw new TypeError("Invalid ICNS entry length");
+      }
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
-      imageOffset += imageHeader[1];
+      imageOffset += imageLength;
     }
     if (images.length === 0) {
       throw new TypeError("Invalid ICNS, no sizes found");
@@ -483,6 +494,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095be85c7a8fd25763895a47c46b4292ebd9c..941c8a4e3e029e28d935e23ca0ab1507b72af2d0 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -116,6 +116,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4cac6abb366378e5feed745f16981b441c0..3617e50f28cfbd1b5e0a7e2fe813204c3b9fc88b 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -114,6 +114,9 @@ function extractPartialStreams(input) {
   while (offset < input.length) {
     const jxlpBox = findBox(input, "jxlp", offset);
     if (!jxlpBox) break;
+    if (jxlpBox.size === 0) {
+      throw new TypeError("Invalid JXL, zero-sized jxlp box");
+    }
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
